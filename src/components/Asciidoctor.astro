---
import Processor from 'asciidoctor';

let { content } = Astro.props;

if (!content) {
    content = "****\nNeed some content!\n****\n";
}

const htmlContent = Processor().convert(content);
---
<article>
    {htmlContent}
</article>